    <nav class='nav-bar'>
        <%= link_to "Home", root_path %>
        <%= link_to "About", about_path  if logged_in?(:site_admin)%>
        <%= link_to "Garage", garage_path if logged_in?(:site_admin) %>
        <% if current_user %>
          <%= link_to "Logout", destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to "Login", new_user_session_path %>
        <% end %>
    </nav>